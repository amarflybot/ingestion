spring.application.name=consumer

spring.cloud.stream.bindings.producer.destination = consumer
spring.cloud.stream.bindings.producer.group=consumer-group
spring.cloud.stream.bindings.producer.durableSubscription=true

server.port=8001